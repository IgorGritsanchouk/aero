import java.math.RoundingMode;
import java.text.DecimalFormat;
import java.text.NumberFormat;

public class MainTest {

    public static void main(String[] args){

        System.out.println("=== Start TEST ===");

        //private static NumberFormat formatterNoDigit = new DecimalFormat("#.").setRoundingMode(RoundingMode.HALF_UP );

        //float value= DecimalFormat("#.##").apply { roundingMode = RoundingMode.HALF_UP }

    }

//    fun main(args: Array<String>) {
//        val f = DecimalFormat("#.##").apply { roundingMode = RoundingMode.HALF_UP }
//        println(f.format(-0.025f))
//        println(f.format(-0.015f))
//        println(f.format(-0.005f))
//        println(f.format(0.005f))
//        println(f.format(0.015f))
//        println(f.format(0.025f))
//    }
}
